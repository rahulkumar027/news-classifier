# News Classifier (Business / Sports / Tech)

Simple TF-IDF + Logistic Regression news classifier with Flask UI.

## Quickstart (Linux/macOS)
1. create venv:
   ```bash
   python -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
